[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    integration: marks tests that exercise multiple backend modules together
    smoke: marks seeded demo smoke coverage
    unit: marks focused unit-level coverage
